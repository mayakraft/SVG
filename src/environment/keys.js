/**
 * SVG (c) Robby Kraft
 */

// frequently-used words
const keys = [
  "number",
  "object",
  "transform",
  "class",
  "style",
  "function",
  "string",
  "undefined",
  "boolean",
  "path",
  "svg",
  "id",
];

const Keys = {};

keys.forEach(key => Keys[key] = key);

export default Keys;
