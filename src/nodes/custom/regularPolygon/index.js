/**
 * SVG (c) Robby Kraft
 */
import args from "./arguments";
import * as K from "../../../environment/keys";

export default {
  regularPolygon: {
    nodeName: "polygon",
    attributes: [K._points],
    args: args
  }
};
