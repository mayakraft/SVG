/**
 * SVG (c) Robby Kraft
 */
import args from "./arguments";
import * as K from "../../../environment/keys";

export default {
  regularPolygon: {
    nodeName: K.polygon,
    attributes: [K.points],
    args: args
  }
};
